version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: mysql-hw
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: product_db
    ports:
      - '3307:3306'
